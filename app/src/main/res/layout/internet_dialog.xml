<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="300dp"
    android:layout_height="400dp"
    android:weightSum="100"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_centerInParent="true"
    android:background="@color/colorPrimaryDark"
    android:layout_gravity="center">

    <TextView
        android:id="@+id/textViewMessage"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_weight="20"
        android:gravity="center"
        android:layout_marginTop="0dp"
        style="@android:style/TextAppearance.DeviceDefault.Medium"
        android:textStyle="bold"
        android:textColor="@color/colorPrimary"
        android:text="@string/checking_internet_connection" />

    <RelativeLayout
        android:id="@+id/idtop"
        android:layout_width="200dp"
        android:layout_height="0dp"
        android:layout_weight="50"
        android:background="@drawable/z_internet">
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/idmiddle"
        android:layout_width="fill_parent"
        android:layout_weight="10"
        android:layout_height="0dp">
        <ProgressBar
            android:id="@+id/simpleProgressBar"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:visibility="visible"
            android:layout_centerHorizontal="true"/>
    </RelativeLayout>

        <Button
            android:id="@+id/startButton"
            android:layout_width="150dp"
            android:layout_height="0dp"
            android:layout_weight="20"
            android:layout_margin="@dimen/letters_margin"
            android:padding="@dimen/letters_margin"
            android:text="@string/reload"
            style="@android:style/TextAppearance.DeviceDefault.Large"
            android:textStyle="bold"
            android:background="@drawable/z_yellowbutton"
            android:gravity="center"
            android:layout_gravity="center"
            android:textColor="@color/colorNavigationBar"/>

</LinearLayout>
